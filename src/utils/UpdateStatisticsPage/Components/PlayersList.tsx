import styles from "../styles/PlayersList.module.scss";

const PlayersList = () => {
  return (
    <>
      <section className={styles.PlayersList}>PlayersList</section>
    </>
  );
};

export default PlayersList;
