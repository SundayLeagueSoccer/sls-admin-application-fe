import styles from "../styles/PageHeader.module.scss";

const PageHeader = () => {
  return (
    <>
      <section className={styles.PageHeader}>PageHeader</section>
    </>
  );
};

export default PageHeader;
